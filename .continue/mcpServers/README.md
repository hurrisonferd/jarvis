# MCP Servers

`.continue/mcpServers/` is the Continue MCP server-config cave.

## Current Role

This folder points Continue agent mode at configured MCP servers such as JARVIS and GBrain.

Keep shareable project config here. Personal secrets and local credentials belong outside git.
