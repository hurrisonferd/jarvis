# Continue

`.continue/` is the Continue.dev tool-config cave.

It contains MCP server config for local/agent-mode Continue workflows.

Keep only shareable project config here. Personal local settings should stay out of git.
