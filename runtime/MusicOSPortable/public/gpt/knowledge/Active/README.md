# MusicOS — Active Knowledge

This folder is the curated home for the currently active MusicOS — The Wizard knowledge pack.

```text
ACTIVE = CURRENT GPT KNOWLEDGE SURFACE
ROOT / LEGACY = SOURCE, ARCHAEOLOGY, OR UNMOVED MATERIAL UNTIL EXPLICITLY CURATED
```

Files should move into `Active/` only through an explicit cleanup/migration pass so manifest paths, builder instructions, tests, and retrieval routes stay deterministic.

Current active-pack semantics are defined by the GPT knowledge manifest. Creating this folder alone does not silently move or retire any existing cartridge.

> Keep the active shelf obvious. Preserve lineage outside it.
