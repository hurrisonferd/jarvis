# Src

`src/` is the TypeScript diagnostics source cave.

## Current Source

| File | Purpose |
| --- | --- |
| `diagnostics.ts` | Connector diagnostics used by `npm run diagnostics` and `npm run validate`. |

Generated output belongs in `dist/`, not here.
