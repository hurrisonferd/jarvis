# Claude Commands

`.claude/commands/` is the Claude command/config cave.

Status: undecided shared-vs-local tooling.

Keep only reusable command definitions here. Do not place secrets, raw transcripts, or private evidence.
