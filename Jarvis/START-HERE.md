# Start Here — JARVIS

This room is the engineering front door for the public JARVIS system.

## Begin with the smallest working proof

1. Run the persistent-memory demo from the current compatibility path under `demos/`.
2. Inspect the public ISO starter under `templates/iso-starter/`.
3. Read `QUICKSTART.md` at the repository root while migration is in progress.
4. Continue into architecture only after the runnable examples make sense.

## Current compatibility routes

The five-room reorganization is staged on a branch. During migration, some active content remains at legacy root paths so workflows, imports, hosted pages, and public links do not break.

| Public function | Current protected route | Future room |
|---|---|---|
| Core architecture | `core/JarvisMain/` | `Jarvis/` with compatibility protection |
| Demos | `demos/` | `Jarvis/demos/` after workflow repair |
| ISO starter | `templates/iso-starter/` | `ISOs/Templates/` after validator repair |
| Browser interface | `docs/` | `Jarvis/docs/` or retained hosted route |
| Gameboy | `app/gameboy/`, `app/emulator/gameboy/` | protected pending exact comparison |
| MusicOS | `MusicOS/`, `runtime/MusicOSPortable/` | protected pending runtime map |

## What belongs here

- runnable JARVIS demonstrations;
- public runtime and architecture;
- sanitized memory examples;
- public systems, packages, tools, and applications;
- technical specifications and recovery documentation.

## What does not belong here

- private crew identity records;
- raw personal chat exports;
- unredacted evidence;
- symbolic or spiritual research better routed to `I Ching/`;
- unrelated projects better routed to `Personal Projects/`.

## Legacy law

`core/JarvisMain/` is a protected historical and operational anchor. It will not be flattened or renamed as a cosmetic cleanup. Any future relocation requires dependency evidence, a compatibility route, and a rollback receipt.

**Status:** migration-safe public guide  
**Steward:** ERIS under Raven authority  
**Last reviewed:** 2026-08-02
