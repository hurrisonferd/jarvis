# Dither — see an image the Game Boy way

**JNL:** CONN-MCP-RT-0049 · **Tool:** `jarvis_dither` · **Connector:** jarvis-mcp

Dither a repo image to the 4-shade Game Boy palette with ordered (Bayer) dithering — the DMG look.

> Ground truth is the `registerTool("jarvis_dither", ...)` block in
> `supabase/functions/jarvis-mcp/index.ts` — this file is its governed mirror (JMS).
