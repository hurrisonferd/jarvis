# Listen — a track's musical features

**JNL:** CONN-MCP-RT-0048 · **Tool:** `jarvis_listen` · **Connector:** jarvis-mcp

Listen to a track by name — returns tempo, key, energy, brightness, mood, and length from the Ears feature pipeline.

> Ground truth is the `registerTool("jarvis_listen", ...)` block in
> `supabase/functions/jarvis-mcp/index.ts` — this file is its governed mirror (JMS).
