---
memory_tier: JLTM
grade: system
---

# The Pinch — squeeze the whole tree (drift, debt, bloat)

**JNL:** CONN-MCP-RT-0047 · **Tool:** `jarvis_pinch` · **Connector:** jarvis-mcp

The world-spell (P3): read PINCH.md in one squeeze — drift (mirror vs HEAD), debt vs structure (real orphans, not the trunk), and GL7 bloat candidates. Tells what needs attention from what's healthy structure.

> Ground truth is the `registerTool("jarvis_pinch", ...)` block in
> `supabase/functions/jarvis-mcp/index.ts` — this file is its governed mirror (JMS).
