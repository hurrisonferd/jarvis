# Eyes — the whole system in one look

**JNL:** CONN-MCP-RT-0046 · **Tool:** `jarvis_eyes` · **Connector:** jarvis-mcp

Jarvis & Ayre's eyes: live state (global mirror) + structure (the wiring map) + vitality (the health audit — orphans, ruleless rules, drift).

> Ground truth is the `registerTool("jarvis_eyes", ...)` block in
> `supabase/functions/jarvis-mcp/index.ts` — this file is its governed mirror (JMS).
