# JIP — list

**JNL:** CONN-MCP-RT-0051 · **Tool:** `jarvis_jip_list` · **Connector:** jarvis-mcp

List JIPs (version history of metadata changes), optionally for one target JD (read-only).

> Ground truth is the `registerTool("jarvis_jip_list", ...)` block in
> `supabase/functions/jarvis-mcp/index.ts` — this file is its governed mirror (JMS).
