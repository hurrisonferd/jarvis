# Tools

Tool mirror documentation cave for the JARVIS MCP connector.

Each file describes one MCP tool surface such as query, recall, repo read, GitHub tree, private tree, status, suit-up, or session open/close.

These are docs/mirrors, not the deployed TypeScript implementation.

See `INDEX.md` for grouped navigation.
