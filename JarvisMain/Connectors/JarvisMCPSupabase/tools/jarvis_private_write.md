# Private — write/scaffold (Jarvis-Private)

**JNL:** CONN-MCP-RT-0040 · **Tool:** `jarvis_private_write` · **Connector:** jarvis-mcp

Write one or many files directly to main of the private storage repo (Jarvis-Private) in one commit — scaffold and store projects (not canon). AEGIS-gated.

> Ground truth is the `registerTool("jarvis_private_write", ...)` block in
> `supabase/functions/jarvis-mcp/index.ts` — this file is its governed mirror (JMS).
