# Architecture

JARVIS architecture, specs, audits, identity, and rebuild cave.

## Main Areas

| Folder | Purpose |
| --- | --- |
| `specs/` | Formal architecture/spec documents. |
| `audits/` | Audit records and stale-config reviews. |
| `rebuild/` | Sanitized rebuild packets and recovery notes. |

Architecture describes the system. Runtime state belongs elsewhere.
