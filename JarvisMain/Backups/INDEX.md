# Backups — Manifests Only

**No actual backup data lives here.** This directory contains manifests and pointers
only. Actual backups are stored in external systems.

| Directory | Purpose |
|-----------|---------|
| `cloud/` | Cloud backup manifest |

See `cloud/` for backup manifest.
