# Contracts

`contracts/` is currently an empty parked cave.

Use it only if formal contracts, interface agreements, or governed autonomy contracts become active source.

Until then, this folder is safe to remove later if Raven confirms it has no intended role.
