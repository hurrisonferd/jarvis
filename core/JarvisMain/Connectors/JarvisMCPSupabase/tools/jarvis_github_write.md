---
memory_tier: JLTM
grade: system
---

# GitHub Write — propose file(s) as one PR

**JNL:** CONN-MCP-RT-0031 · **Tool:** `jarvis_github_write` · **Connector:** jarvis-mcp

Write one or many files to the repo as a single pull request — never directly to protected main. AEGIS-gated.

> Ground truth is the `registerTool("jarvis_github_write", ...)` block in
> `core/supabase/functions/jarvis-mcp/index.ts` — this file is its governed mirror (JMS).
