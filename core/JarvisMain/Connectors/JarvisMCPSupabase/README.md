# Jarvis MCP Supabase

Git-backed MCP connector documentation cave.

## Main Areas

| Path | Purpose |
| --- | --- |
| `tools/` | Tool mirror docs for the deployed JARVIS MCP connector. |
| `JMCPSB-0001` | Connector source/manifest artifact. |

The deployed runtime lives in Supabase. This folder helps rebuild and inspect it from git.
