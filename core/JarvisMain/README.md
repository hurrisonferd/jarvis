# JarvisMain

`core/JarvisMain/` is the system canon cave.

## Main Areas

| Path | Purpose |
| --- | --- |
| `Architecture/` | Specs, identity, audits, rebuild records, and governing architecture. |
| `Connectors/` | Git-backed connector/tool mirror docs. |
| `Implementation/` | Task/handoff implementation records. |
| `Manual/` | Human-readable operating manual and event history. |

## Boundary

This is canon and architecture, not runtime state.

When in doubt, index here and execute elsewhere.
