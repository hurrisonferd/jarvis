# Self-Deletion API Diagnostic Report

**Test Date:** 2026-06-28  
**Test Time:** 00:47 UTC  
**Agent Canvas Version:** 1.29.0  
**API Endpoint:** `http://127.0.0.1:60000/api/conversations/{id}`

---

## Executive Summary

✅ **Self-Deletion API is WORKING correctly.**

| Test | Result | HTTP Code |
|------|--------|-----------|
| Delete existing conversation | ✅ SUCCESS | 200 |
| Conversation disappears from list | ✅ VERIFIED | - |
| Invalid API key | ✅ Returns 401 Unauthorized | 401 |
| Missing API key | ✅ Returns 401 Unauthorized | 401 |
| Non-existent conversation ID | ✅ Returns 400 Bad Request | 400 |

---

## Test 1: Delete Existing Conversation

### Command
```bash
curl -X DELETE http://127.0.0.1:60000/api/conversations/{conversation_id} \
  -H "X-Session-API-Key: $SESSION_API_KEY"
```

### Before Deletion
```json
{
  "id": "7c22b77e-fde1-430b-b520-322167e31aa2",
  "title": "Hello",
  "execution_status": "error"
}
```

### Response
```json
{"success":true}
```

**HTTP Status Code:** 200 OK

### Verification
- Conversation folder removed from `/workspace/conversations/`
- API returns 404 when querying deleted conversation ID

---

## Test 2: Original Command Pattern (Using ls | head -1)

### Command
```bash
curl -X DELETE http://127.0.0.1:60000/api/conversations/$(ls /workspace/conversations/ | head -1) \
  -H "X-Session-API-Key: $SESSION_API_KEY"
```

### Result
✅ **SUCCESS** - Returns `{"success":true}` with HTTP 200

---

## Test 3: Error Scenarios

### 3.1 Invalid API Key
```bash
curl -X DELETE "http://127.0.0.1:60000/api/conversations/test-id" \
  -H "X-Session-API-Key: invalid-key"
```

**Response:**
```json
{"detail":"Unauthorized"}
```
**HTTP Code:** 401

### 3.2 Missing API Key
```bash
curl -X DELETE "http://127.0.0.1:60000/api/conversations/test-id"
```

**Response:**
```json
{"detail":"Unauthorized"}
```
**HTTP Code:** 401

### 3.3 Non-Existent Conversation ID
```bash
curl -X DELETE "http://127.0.0.1:60000/api/conversations/00000000-0000-0000-0000-000000000000" \
  -H "X-Session-API-Key: $SESSION_API_KEY"
```

**Response:**
```json
{"detail":"Bad Request"}
```
**HTTP Code:** 400

---

## Test 4: Conversation List Verification

### Before Deletion
```
/workspace/conversations/
├── 68cfc9f5be904b1293ef9527629b1c31  (original conversation)
└── 7c22b77efde1430bb520322167e31aa2  (test sandbox)
```

### After Deletion
```
/workspace/conversations/
└── 68cfc9f5be904b1293ef9527629b1c31  (only original remains)
```

---

## Conclusions

1. **API returns success:** ✅ Returns `{"success":true}` with HTTP 200
2. **Conversation disappears:** ✅ Verified - folder removed, API returns 404
3. **Error messages on failure:** ✅ Properly returns:
   - 401 Unauthorized for auth failures
   - 400 Bad Request for invalid conversation IDs

## Notes

- The API requires a valid `X-Session-API-Key` header
- Conversation IDs are UUIDs (format: `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`)
- The filesystem folder in `/workspace/conversations/` is removed upon deletion
- The API endpoint follows RESTful conventions (DELETE /api/conversations/{id})

---

*Generated by OpenHands Agent on behalf of Co-op Worker*