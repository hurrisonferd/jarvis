# Mnemos

`mnemos/` is the local semantic-memory helper cave.

## Source

| File | Purpose |
| --- | --- |
| `mnemos_vector.py` | SQLite + Ollama embedding helper for local JARVIS memory experiments. |

## Boundary

This is local helper code, not the authoritative cloud memory spine.

Ollama must be running for embedding operations that use `nomic-embed-text`.
